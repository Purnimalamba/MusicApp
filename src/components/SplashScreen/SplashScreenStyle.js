import {StyleSheet} from 'react-native';
import Colours from '../../Utils/ColourConstants';

export const styles = StyleSheet.create({
  mainContainer: {
    backgroundColor: Colours.RoyalBlue,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  image: {
    width: 70,
    height: 70,
  },
});
